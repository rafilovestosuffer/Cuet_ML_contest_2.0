"""Torch Datasets for text / image / multimodal inputs (stub).

TODO (Claude Code): implement DisasterTextDataset, DisasterImageDataset, and
DisasterMultimodalDataset, reading data/folds/folds_canonical.csv and the image
folders. Use transforms.py for images and tokenization for text (max_len=256).
"""
